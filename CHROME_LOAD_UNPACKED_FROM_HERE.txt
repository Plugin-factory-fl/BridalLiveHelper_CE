Install BridalLive Helper from the dist/ folder after building:

  npm install
  npm run build

Then in Chrome: chrome://extensions → Developer mode → Load unpacked → choose dist/

Open https://app.bridallive.com and click the BL button (or the Helper toolbar icon).
