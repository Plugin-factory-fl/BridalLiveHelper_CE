{
  "name": "bridallive-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "package:zip": "npm run build && node scripts/zip-dist.mjs",
    "mock-catalog:import": "node scripts/import-mock-catalog.mjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3",
    "vite": "^6.0.11"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
